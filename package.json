{
  "name": "vote-observer-registration",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && cd server && npx prisma generate",
    "start": "cd server && npx prisma migrate deploy && npx prisma db seed && node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
